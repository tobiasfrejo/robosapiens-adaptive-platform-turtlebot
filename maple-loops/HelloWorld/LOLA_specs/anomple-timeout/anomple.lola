in anompleClock: Str
out timeout: Bool
out acc : Int
out t : Str
t = anompleClock
acc = if t[-1, ""] == "anom" && t == "timer" then 1
      else if !(acc[-1, 0] <= 0) && t == "timer" then acc[-1, 0] + 1
      else if t == "end_e" then 0
      else acc[-1, 0]
timeout = !(acc <= 10)